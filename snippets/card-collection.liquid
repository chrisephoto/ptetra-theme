<div class="card">
    <div class="upper">
        {{ card_collection.featured_image | image_url | image_tag, width: 200, height: 200 }}
    </div>
    <div class="lower">
        {{  card_collection.title }}
        {{  card_collection.description }}
    </div>
</div>